pipeline {
    agent any 
    
    stages{
        stage('docker pull image') {
            steps {
                sh 'docker pull kpun-be:lastest'
                sh 'docker pull kpun-fe:lastest'
            }
        }
    }
}